#include "Board.hpp"
#include <list>

std::list<Board> Board::generate_white_moves(int depth) {
    // check castle possibilities
    // do pawn movements (en passant and promotion) (promotions only become knights or queens)
    // do king, knight, 
}